// -----------------------------------------------------------------------------
// This file was automatically generated by dawncore-apigen.
// DO NOT EDIT MANUALLY.
//
// Generated At: 2025-09-20 08:11:06.644894678 UTC
// Branch: Releases/15.18
// Version: 15.18.711.2000
//
// -----------------------------------------------------------------------------

#[allow(unused_imports)]
use crate::types::*;
use crate::AuthenticatedHttp as __AuthenticatedHttp;
pub async fn lol_store_v_2_offers_get(
    client: __AuthenticatedHttp,
    type_id: std::option::Option<std::string::String>,
) -> reqwest::Result<std::vec::Vec<LolStoreCapOffer>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v2/offers");
        if let Some(type_id) = &type_id {
            let _value = &type_id;
            url.push_str(format!("typeId={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "typeId" : type_id }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_by_page_type_get(
    client: __AuthenticatedHttp,
    page_type: std::string::String,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = format!("/lol-store/v1/{page_type}", page_type = page_type);
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_get_store_url_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::string::String> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/getStoreUrl");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_last_page_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::string::String> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/lastPage");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_payment_details_get(
    client: __AuthenticatedHttp,
    action: std::string::String,
    gift_recipient_account_id: std::option::Option<u64>,
    gift_message: std::option::Option<std::string::String>,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/paymentDetails");
        {
            let _value = &action;
            url.push_str(format!("action={}", _value).as_str());
        }
        if let Some(gift_recipient_account_id) = &gift_recipient_account_id {
            let _value = &gift_recipient_account_id;
            url.push_str(format!("giftRecipientAccountId={}", _value).as_str());
        }
        if let Some(gift_message) = &gift_message {
            let _value = &gift_message;
            url.push_str(format!("giftMessage={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(
            &{
                serde_json::json!(
                    { "action" : action, "giftRecipientAccountId" :
                    gift_recipient_account_id, "giftMessage" : gift_message }
                )
            },
        );
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_giftablefriends_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::vec::Vec<LolStoreGiftingFriend>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/giftablefriends");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_status_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<LolStoreStoreStatus> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/status");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_item_keys_from_instance_ids_get(
    client: __AuthenticatedHttp,
    instance_ids: std::vec::Vec<std::string::String>,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/itemKeysFromInstanceIds");
        {
            let _value = instance_ids
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("instanceIds={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "instanceIds" : instance_ids }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_order_notifications_by_id_get(
    client: __AuthenticatedHttp,
    id: u64,
) -> reqwest::Result<LolStoreOrderNotificationResource> {
    let url = {
        let mut url = format!("/lol-store/v1/order-notifications/{id}", id = id);
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_3_purchase_post(
    client: __AuthenticatedHttp,
    items: std::vec::Vec<LolStoreItemOrderDto>,
) -> reqwest::Result<LolStorePurchaseOrderResponseDto> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v3/purchase");
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "items" : items }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_alias_change_notifications_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::vec::Vec<LolStoreAliasChangeNotificationResource>> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-store/v1/alias-change-notifications",
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_offers_get(
    client: __AuthenticatedHttp,
    inventory_type_uui_ds: std::option::Option<std::vec::Vec<std::string::String>>,
) -> reqwest::Result<std::vec::Vec<LolStoreCapOffer>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/offers");
        if let Some(inventory_type_uui_ds) = &inventory_type_uui_ds {
            let _value = inventory_type_uui_ds
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryTypeUUIDs={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "inventoryTypeUUIDs" : inventory_type_uui_ds }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_skins_by_skin_id_get(
    client: __AuthenticatedHttp,
    skin_id: i32,
) -> reqwest::Result<LolStoreCatalogItem> {
    let url = {
        let mut url = format!("/lol-store/v1/skins/{skin_id}", skin_id = skin_id);
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_catalog_sales_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::vec::Vec<LolStoreItemSale>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/catalog/sales");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_last_page_post(
    client: __AuthenticatedHttp,
    page_type: std::string::String,
) -> reqwest::Result<()> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/lastPage");
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "pageType" : page_type }) });
    let response = builder.send().await?;
    Ok(())
}
pub async fn lol_store_v_1_catalog_by_instance_ids_get(
    client: __AuthenticatedHttp,
    instance_ids: std::vec::Vec<std::string::String>,
) -> reqwest::Result<std::vec::Vec<LolStoreCatalogItem>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/catalogByInstanceIds");
        {
            let _value = instance_ids
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("instanceIds={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "instanceIds" : instance_ids }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_store_ready_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<bool> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/store-ready");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_order_notifications_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::vec::Vec<LolStoreOrderNotificationResource>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/order-notifications");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_catalog_get(
    client: __AuthenticatedHttp,
    inventory_type: std::option::Option<std::vec::Vec<std::string::String>>,
    item_id: std::option::Option<std::vec::Vec<i32>>,
) -> reqwest::Result<std::vec::Vec<LolStoreCatalogItem>> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/catalog");
        if let Some(inventory_type) = &inventory_type {
            let _value = inventory_type
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryType={}", _value).as_str());
        }
        if let Some(item_id) = &item_id {
            let _value = item_id
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("itemId={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(
            &{
                serde_json::json!(
                    { "inventoryType" : inventory_type, "itemId" : item_id }
                )
            },
        );
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_offers_by_offer_id_get(
    client: __AuthenticatedHttp,
    offer_id: std::string::String,
) -> reqwest::Result<LolStoreCapOffer> {
    let url = {
        let mut url = format!("/lol-store/v1/offers/{offer_id}", offer_id = offer_id);
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_notifications_acknowledge_post(
    client: __AuthenticatedHttp,
    id: std::string::String,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-store/v1/notifications/acknowledge",
        );
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "id" : id }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_catalog_items_skip_cache_get(
    client: __AuthenticatedHttp,
    catalog_item_keys: std::vec::Vec<LolStoreItemKey>,
) -> reqwest::Result<std::vec::Vec<LolStoreCatalogItem>> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-store/v1/catalog/items/skip-cache",
        );
        {
            let _value = catalog_item_keys
                .iter()
                .map(|value| { format!("{}", serde_json::to_string(& value).unwrap()) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("catalogItemKeys={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "catalogItemKeys" : catalog_item_keys }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_catalog_by_inventory_type_get(
    client: __AuthenticatedHttp,
    inventory_type: std::string::String,
    item_ids: std::vec::Vec<i32>,
) -> reqwest::Result<std::vec::Vec<LolStoreCatalogItem>> {
    let url = {
        let mut url = format!(
            "/lol-store/v1/catalog/{inventory_type}", inventory_type = inventory_type
        );
        {
            let _value = item_ids
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("itemIds={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "itemIds" : item_ids }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_store_v_1_item_keys_from_offer_ids_get(
    client: __AuthenticatedHttp,
    offer_ids: std::vec::Vec<std::string::String>,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/lol-store/v1/itemKeysFromOfferIds");
        {
            let _value = offer_ids
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("offerIds={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "offerIds" : offer_ids }) });
    let response = builder.send().await?;
    response.json().await
}
