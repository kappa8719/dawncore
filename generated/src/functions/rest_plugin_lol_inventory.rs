// -----------------------------------------------------------------------------
// This file was automatically generated by dawncore-apigen.
// DO NOT EDIT MANUALLY.
//
// Generated At: 2025-09-20 08:11:06.644894678 UTC
// Branch: Releases/15.18
// Version: 15.18.711.2000
//
// -----------------------------------------------------------------------------

#[allow(unused_imports)]
use crate::types::*;
use crate::AuthenticatedHttp as __AuthenticatedHttp;
pub async fn lol_inventory_v_2_inventory_by_inventory_type_get(
    client: __AuthenticatedHttp,
    inventory_type: std::string::String,
) -> reqwest::Result<std::vec::Vec<LolInventoryInventoryItemWithPayload>> {
    let url = {
        let mut url = format!(
            "/lol-inventory/v2/inventory/{inventory_type}", inventory_type =
            inventory_type
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_inventory_emotes_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::vec::Vec<LolInventoryInventoryItemWithPayload>> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/inventory/emotes");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_xbox_subscription_status_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<LolInventoryXboxSubscriptionStatus> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/xbox-subscription-status",
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_signed_wallet_get(
    client: __AuthenticatedHttp,
    currency_types: std::vec::Vec<std::string::String>,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/signedWallet");
        {
            let _value = currency_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("currencyTypes={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "currencyTypes" : currency_types }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_inventory_get(
    client: __AuthenticatedHttp,
    inventory_types: std::vec::Vec<std::string::String>,
) -> reqwest::Result<std::vec::Vec<LolInventoryInventoryItemWithPayload>> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/inventory");
        {
            let _value = inventory_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryTypes={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "inventoryTypes" : inventory_types }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_wallet_by_currency_type_get(
    client: __AuthenticatedHttp,
    currency_type: std::string::String,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = format!(
            "/lol-inventory/v1/wallet/{currency_type}", currency_type = currency_type
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_wallet_get(
    client: __AuthenticatedHttp,
    currency_types: std::vec::Vec<std::string::String>,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/wallet");
        {
            let _value = currency_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("currencyTypes={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "currencyTypes" : currency_types }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_signed_inventory_cache_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/signedInventoryCache",
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_signed_inventory_simple_get(
    client: __AuthenticatedHttp,
    inventory_types: std::vec::Vec<std::string::String>,
    query_params: std::option::Option<serde_json::Value>,
) -> reqwest::Result<std::string::String> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/signedInventory/simple",
        );
        {
            let _value = inventory_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryTypes={}", _value).as_str());
        }
        if let Some(query_params) = &query_params {
            let _value = &query_params;
            url.push_str(format!("queryParams={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(
            &{
                serde_json::json!(
                    { "inventoryTypes" : inventory_types, "queryParams" : query_params }
                )
            },
        );
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_inventory_with_f_2_p_get(
    client: __AuthenticatedHttp,
    inventory_types: std::vec::Vec<std::string::String>,
) -> reqwest::Result<std::vec::Vec<LolInventoryInventoryItemWithPayload>> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/inventoryWithF2P");
        {
            let _value = inventory_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryTypes={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "inventoryTypes" : inventory_types }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_notifications_by_inventory_type_get(
    client: __AuthenticatedHttp,
    inventory_type: std::string::String,
) -> reqwest::Result<std::vec::Vec<LolInventoryInventoryNotification>> {
    let url = {
        let mut url = format!(
            "/lol-inventory/v1/notifications/{inventory_type}", inventory_type =
            inventory_type
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_signed_inventory_get(
    client: __AuthenticatedHttp,
    inventory_types: std::vec::Vec<std::string::String>,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/signedInventory");
        {
            let _value = inventory_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryTypes={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "inventoryTypes" : inventory_types }) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_champ_select_inventory_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::string::String> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/champSelectInventory",
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_cherry_inventory_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::string::String> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/cherryInventory");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_notification_acknowledge_post(
    client: __AuthenticatedHttp,
    id: i64,
) -> reqwest::Result<()> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/notification/acknowledge",
        );
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({ "id" : id }) });
    let response = builder.send().await?;
    Ok(())
}
pub async fn lol_inventory_v_1_signed_inventory_tournamentlogos_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/signedInventory/tournamentlogos",
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_signed_wallet_by_currency_type_get(
    client: __AuthenticatedHttp,
    currency_type: std::string::String,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = format!(
            "/lol-inventory/v1/signedWallet/{currency_type}", currency_type =
            currency_type
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_initial_configuration_complete_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<bool> {
    let url = {
        let mut url = std::string::String::from(
            "/lol-inventory/v1/initial-configuration-complete",
        );
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_strawberry_inventory_get(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::string::String> {
    let url = {
        let mut url = std::string::String::from("/lol-inventory/v1/strawberryInventory");
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn lol_inventory_v_1_players_by_puuid_inventory_get(
    client: __AuthenticatedHttp,
    puuid: std::string::String,
    inventory_types: std::vec::Vec<std::string::String>,
) -> reqwest::Result<std::vec::Vec<LolInventoryInventoryItemWithPayload>> {
    let url = {
        let mut url = format!(
            "/lol-inventory/v1/players/{puuid}/inventory", puuid = puuid
        );
        {
            let _value = inventory_types
                .iter()
                .map(|value| { format!("{}", & value) })
                .collect::<Vec<_>>()
                .join(",");
            url.push_str(format!("inventoryTypes={}", _value).as_str());
        }
        url
    };
    let mut builder = client.get(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "inventoryTypes" : inventory_types }) });
    let response = builder.send().await?;
    response.json().await
}
