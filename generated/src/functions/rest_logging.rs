// -----------------------------------------------------------------------------
// This file was automatically generated by dawncore-apigen.
// DO NOT EDIT MANUALLY.
//
// Generated At: 2025-09-20 08:11:06.644894678 UTC
// Branch: Releases/15.18
// Version: 15.18.711.2000
//
// -----------------------------------------------------------------------------

#[allow(unused_imports)]
use crate::types::*;
use crate::AuthenticatedHttp as __AuthenticatedHttp;
pub async fn logging_get_entries(
    client: __AuthenticatedHttp,
) -> reqwest::Result<std::vec::Vec<LogEvent>> {
    let url = {
        let mut url = std::string::String::from("/LoggingGetEntries");
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn logging_metrics(
    client: __AuthenticatedHttp,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/LoggingMetrics");
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn logging_start(
    client: __AuthenticatedHttp,
    buffered: std::option::Option<bool>,
    severity: std::option::Option<LogSeverityLevels>,
) -> reqwest::Result<()> {
    let url = {
        let mut url = std::string::String::from("/LoggingStart");
        if let Some(buffered) = &buffered {
            let _value = &buffered;
            url.push_str(format!("buffered={}", _value).as_str());
        }
        if let Some(severity) = &severity {
            let _value = severity.name();
            url.push_str(format!("severity={}", _value).as_str());
        }
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(&{ serde_json::json!({ "buffered" : buffered, "severity" : severity }) });
    let response = builder.send().await?;
    Ok(())
}
pub async fn logging_metrics_metadata(
    client: __AuthenticatedHttp,
) -> reqwest::Result<serde_json::Value> {
    let url = {
        let mut url = std::string::String::from("/LoggingMetricsMetadata");
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    response.json().await
}
pub async fn riotclient_addorupdatemetric_post(
    client: __AuthenticatedHttp,
    group: std::string::String,
    object: std::string::String,
    name: std::string::String,
    value: u64,
) -> reqwest::Result<()> {
    let url = {
        let mut url = std::string::String::from("/riotclient/addorupdatemetric");
        {
            let _value = &group;
            url.push_str(format!("group={}", _value).as_str());
        }
        {
            let _value = &object;
            url.push_str(format!("object={}", _value).as_str());
        }
        {
            let _value = &name;
            url.push_str(format!("name={}", _value).as_str());
        }
        {
            let _value = &value;
            url.push_str(format!("value={}", _value).as_str());
        }
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder
        .json(
            &{
                serde_json::json!(
                    { "group" : group, "object" : object, "name" : name, "value" : value
                    }
                )
            },
        );
    let response = builder.send().await?;
    Ok(())
}
pub async fn logging_stop(client: __AuthenticatedHttp) -> reqwest::Result<()> {
    let url = {
        let mut url = std::string::String::from("/LoggingStop");
        url
    };
    let mut builder = client.post(client.host.join(url.as_str()).unwrap());
    builder = builder.json(&{ serde_json::json!({}) });
    let response = builder.send().await?;
    Ok(())
}
